---
import { SITE } from '../data/site';
const pathname = Astro.url.pathname;
---

<header id="site-header" class="fixed top-0 left-0 right-0 z-50 transition-all duration-300">
  <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">

    <a href="/" aria-label="The Lobby Tiki Bar — Home" class="flex-shrink-0">
      <img src="/images/logo-cream.png" alt="The Lobby Tiki Bar Oceanside California"
        class="h-12 w-auto" width="180" height="48" />
    </a>

    <nav class="hidden lg:flex items-center gap-8" aria-label="Main navigation">
      {SITE.nav.map((item) => (
        <a
          href={item.href}
          class:list={[
            'font-[family-name:var(--font-stamp)] text-sm tracking-widest uppercase transition-colors duration-200',
            pathname === item.href || pathname.startsWith(item.href + '/')
              ? 'text-[var(--color-amber)]'
              : 'text-[var(--color-sand)] hover:text-[var(--color-amber)]',
          ]}
        >
          {item.label}
        </a>
      ))}
    </nav>

    <div class="hidden lg:flex items-center gap-4">
      <a href={`tel:${SITE.phone.tel}`}
        class="font-[family-name:var(--font-stamp)] text-sm tracking-wider text-sand-muted hover:text-[var(--color-amber)] transition-colors">
        {SITE.phone.display}
      </a>
      <a href={SITE.order.url} target="_blank" rel="noopener noreferrer"
        class="border border-[var(--color-sand)]/40 text-[var(--color-sand)] font-[family-name:var(--font-stamp)] text-sm tracking-widest uppercase px-4 py-2 hover:border-[var(--color-amber)] hover:text-[var(--color-amber)] transition-colors duration-200">
        Order
      </a>
      <a href={SITE.reservations.url} target="_blank" rel="noopener noreferrer"
        class="bg-[var(--color-amber)] text-[var(--color-espresso)] font-[family-name:var(--font-stamp)] text-sm tracking-widest uppercase px-5 py-2 hover:bg-[var(--color-sand)] transition-colors duration-200">
        Reserve
      </a>
    </div>

    <button id="mobile-menu-btn" class="lg:hidden text-[var(--color-sand)] p-2"
      aria-label="Open menu" aria-expanded="false">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>
</header>

<div id="mobile-menu"
  class="fixed inset-0 z-40 bg-[var(--color-espresso)] flex flex-col items-center justify-center gap-8 translate-x-full transition-transform duration-300 lg:hidden"
  aria-hidden="true">
  <button id="mobile-menu-close" class="absolute top-6 right-6 text-[var(--color-sand)] p-2" aria-label="Close menu">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
    </svg>
  </button>
  {SITE.nav.map((item) => (
    <a href={item.href}
      class="font-[family-name:var(--font-stamp)] text-2xl tracking-widest uppercase text-[var(--color-sand)] hover:text-[var(--color-amber)] transition-colors">
      {item.label}
    </a>
  ))}
  <a href={SITE.order.url} target="_blank" rel="noopener noreferrer"
    class="border border-[var(--color-sand)]/40 text-[var(--color-sand)] font-[family-name:var(--font-stamp)] text-sm tracking-widest uppercase px-8 py-3 hover:border-[var(--color-amber)] hover:text-[var(--color-amber)] transition-colors">
    Order Online
  </a>
  <a href={SITE.reservations.url} target="_blank" rel="noopener noreferrer"
    class="mt-2 bg-[var(--color-amber)] text-[var(--color-espresso)] font-[family-name:var(--font-stamp)] text-sm tracking-widest uppercase px-8 py-3 hover:bg-[var(--color-sand)] transition-colors">
    Reserve a Table
  </a>
</div>

<script>
  const header = document.getElementById('site-header');
  const btn = document.getElementById('mobile-menu-btn');
  const closeBtn = document.getElementById('mobile-menu-close');
  const menu = document.getElementById('mobile-menu');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 60) {
      header?.classList.add('nav-scrolled');
    } else {
      header?.classList.remove('nav-scrolled');
    }
  });

  btn?.addEventListener('click', () => {
    menu?.classList.remove('translate-x-full');
    menu?.setAttribute('aria-hidden', 'false');
    btn.setAttribute('aria-expanded', 'true');
  });

  closeBtn?.addEventListener('click', () => {
    menu?.classList.add('translate-x-full');
    menu?.setAttribute('aria-hidden', 'true');
    btn?.setAttribute('aria-expanded', 'false');
  });
</script>
